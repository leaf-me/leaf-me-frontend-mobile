import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        maxHeight: 360,
        minHeight: 360
        // old value ^^ 520
    }
})

export default styles