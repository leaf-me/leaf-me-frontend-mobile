import { StyleSheet } from "react-native"

export default styles = StyleSheet.create({
    viewContainer: {
        backgroundColor: 'red',
        maxHeight: 200,
    },
    pendingContainer: {
        height: 135,
    },
    completeContainer: {
        height: 450
    }

})